# RFS Command Governance

## Purpose

PT13 closes the TUI documentation cycle and keeps the command-surface boundary explicit.
This phase is documentation-only.
It does not add commands.
It does not change runtime behavior.
It does not touch `Rufus.RCK.Core`.
It does not migrate legacy agenting.
It does not deprecate bridges yet.
It does not create a `ModelRouter`.

Canonical UX reference: [`RFS_TUI_UX_CONTRACT.md`](RFS_TUI_UX_CONTRACT.md).
Canonical TUI recording reference: [`RFS_TUI_RECORDING_CONTRACT.md`](RFS_TUI_RECORDING_CONTRACT.md).
Canonical Simple Context reference: [`RFS_SIMPLE_CONTEXT_CONTRACT.md`](RFS_SIMPLE_CONTEXT_CONTRACT.md).

The governed backend pipeline remains relevant behind the TUI, but it is not the primary UX anymore.

North Star for the governed pipeline:

```text
Prompt
  -> Intent
  -> TraceSliceProposal
  -> RFS validation
  -> TraceSlice final
  -> ContextPack
  -> LLM principal
```

Principle:

- the agent proposes;
- RFS validates;
- RCK remembers;
- ContextPack materializes;
- the main LLM consumes.

Hardening rule:
- proposal LLM output is never authoritative;
- validation stays on the RFS side;
- no LLM output becomes a TraceSlice or ContextPack without RFS validation;
- RFS validation keeps unsafe materialization flags disabled unless explicitly decided otherwise.

## 1. Baseline stable commands

These commands are the current baseline surface. They are not experimental.

| Command | Writes RCK | Transport / backend | Notes |
| --- | --- | --- | --- |
| `rfs init` | yes | workspace bootstrap | Initializes `.rfs/` and seeds genesis state/anchor. |
| `rfs status` | no | workspace / Git inspection | Reports workspace, RCK, and Git context. |
| `rfs log` | no | RCK traversal | Walks the active chain from `.rfs/rck/HEAD`. |
| `rfs context-pack` | no | workspace projection | Full ContextPack export. |
| `rfs model get` | no | local config | Reads `.rfs/config.json` when present. |
| `rfs model set <model>` | no | local config | Writes `.rfs/config.json` only; does not write RCK. |
| `rfs model list` | no | Pi RPC | Uses Pi RPC to list available models. |
| `rfs ask <prompt>` | no | Pi JSON mode | Headless ask path. |
| `rfs ask --record <prompt>` | yes | Pi JSON mode | Headless ask path plus controlled RCK recording. |
| `rfs intent <prompt>` | no | deterministic AgentTask | Intent inference only. |
| `rfs intent --record <prompt>` | yes | deterministic AgentTask | Intent inference plus controlled RCK recording. |

## 2. TraceSlice / ContextPack pipelines

### 2.1 Baseline deterministic

Recommended when you want the current deterministic baseline without LLM proposal hardening:

```text
rfs trace-slice "<prompt>"
rfs context-pack --trace-slice "<prompt>"
```

Meaning:

- `rfs trace-slice` produces the deterministic TraceSlice baseline.
- `rfs context-pack --trace-slice` materializes a ContextPack from that deterministic slice.
- The TraceSlice is the selection plan.
- The ContextPack is the materialized projection.
- The main LLM is only the downstream consumer.

### 2.2 Governed deterministic pipeline

Recommended when you want an auditable proposal/validation boundary before materialization:

```text
rfs trace-slice-proposal "<prompt>"
rfs trace-slice-validate "<prompt>"
rfs context-pack --trace-slice-validated "<prompt>"
```

Meaning:

- `rfs trace-slice-proposal` proposes a candidate TraceSlice.
- `rfs trace-slice-validate` is the authority boundary: RFS validates the proposal and emits the final TraceSlice.
- `rfs context-pack --trace-slice-validated` materializes a ContextPack from the validated TraceSlice.
- Proposal is not the final decision.
- Validation is the final decision.
- ContextPack materializes the validated decision.

### 2.3 LLM experimental proposal pipeline

When the proposal is generated by Pi / LLM experimentation, the command surface is:

```text
rfs trace-slice-proposal-llm "<prompt>"
rfs trace-slice-validate-llm "<prompt>"
```

Meaning:

- the LLM only proposes;
- RFS validation remains the authority;
- `trace-slice-proposal-llm` is proposal-only;
- `trace-slice-validate-llm` still validates before emitting a final TraceSlice;
- proposal output does not replace validation;
- proposal output does not materialize a ContextPack.

## 3. Experimental / diagnostic commands

These commands are valid, but they remain part of the stabilization pipeline.
Experimental does not mean broken; it means the pipeline is still being hardened.

| Command | Status | Transport / backend | Notes |
| --- | --- | --- | --- |
| `rfs ask-json <prompt>` | experimental / diagnostic | Pi JSON mode | Diagnostic JSON-mode ask path. |
| `rfs agent-json <task>` | experimental / diagnostic | Pi JSON/tools | Experimental agent JSON path. |
| `rfs trace-slice-proposal <prompt>` | experimental | deterministic proposal pipeline | Proposal only; not authoritative. |
| `rfs trace-slice-validate <prompt>` | experimental | deterministic proposal pipeline + RFS validation | Produces the final validated TraceSlice. |
| `rfs trace-slice-proposal-llm <prompt>` | experimental | Pi / LLM proposal path | LLM proposes only. |
| `rfs trace-slice-validate-llm <prompt>` | experimental | Pi / LLM proposal path + RFS validation | LLM proposes, RFS validates. |
| `rfs context-pack --trace-slice-validated <prompt>` | experimental | validated TraceSlice materialization | Focused ContextPack from the validated slice. |

## 4. Legacy / no-migrate-yet commands

These are the current agent paths. They are legacy in the sense that they are not the preferred long-term bridge design, but they are not deprecated yet.

| Command | Status | Transport / backend | Notes |
| --- | --- | --- | --- |
| `rfs agent <task>` | legacy current | legacy bridge | Current agent path; not deprecated yet. |
| `rfs agent --record <task>` | legacy current | legacy bridge | Current agent path plus controlled RCK recording; not deprecated yet. |

Important:

- these remain the current path for agent execution;
- they are not migrated yet;
- they are not deprecated yet;
- bridge deprecation is a later phase.

## 5. What writes RCK and what does not

### Writes RCK

- `rfs ask --record`
- `rfs intent --record`
- `rfs agent --record`
- TUI internal `/anchor "name"` milestone command

### Does not write RCK

- `/status`
- `/log`
- `/model`
- `/context`
- `/trace`
- `/help`
- `/exit`
- `/model <model>` updates `.rfs/config.json` only
- `rfs ask-json`
- `rfs intent`
- `rfs trace-slice`
- `rfs trace-slice-proposal`
- `rfs trace-slice-validate`
- `rfs trace-slice-proposal-llm`
- `rfs trace-slice-validate-llm`
- `rfs context-pack`
- `rfs context-pack --trace-slice`
- `rfs context-pack --trace-slice-validated`
- `rfs model get`
- `rfs model set`
- `rfs model list`
- `rfs status`
- `rfs log`

Note:

- `rfs model set` writes `.rfs/config.json`.
- `rfs model set` does not write RCK.

## 6. Pi / RPC / JSONL / legacy map

| Command | Transport / backend |
| --- | --- |
| `rfs model list` | Pi RPC |
| `rfs ask` | Pi JSON mode |
| `rfs ask --record` | Pi JSON mode |
| `rfs ask-json` | Pi JSON mode, diagnostic |
| `rfs trace-slice-proposal-llm` | Pi / LLM experimental |
| `rfs trace-slice-validate-llm` | Pi / LLM experimental + RFS validation |
| `rfs agent` | legacy bridge |
| `rfs agent --record` | legacy bridge |
| `rfs agent-json` | Pi JSON/tools experimental |
| `rfs pi` | passthrough / TUI |

Complete-mode transport note:

- the Pi JSON runner keeps argv for prompts at or below 32000 chars
- it switches to stdin above that threshold to avoid `Argument list too long`
- this is a transport safeguard, not a change to the model context window, ContextPack, or RCK recording rules
- external validation in `ChessBoardApp` confirmed the long-prompt path with ~398k chars / ~99k tokens, LLM response, and State + Delta creation
- remaining risks are budget-policy questions, not argv transport; if a future phase wants efficiency or stricter bounds, it can add warning/compact/degrade policy separately

## 7. North Star and anti-drift rules

- Do not touch RCK Core unless there is an explicit architectural decision.
- Do not create `ModelRouter`.
- Do not migrate the legacy agent path yet.
- Do not deprecate bridges yet.
- Do not confuse proposal with final TraceSlice.
- Do not confuse TraceSlice with ContextPack.
- Do not persist raw outputs when a summary, reference, or controlled payload is sufficient.
- Do not add new commands before closing the current cycle.
- Keep `Rufus.Agenting` above Core.
- Keep RFS as the validator and orchestrator.
- Keep ContextPack as a projection, not the storage model.

## 8. Stop points

The immediate closeout sequence for this command-governance cycle is:

- P22 — Consolidation / Command Governance
- P23 — LLM Proposal Hardening
- P24 — Final Handoff / Stop Point

Current status:

- PT13 is the documentation stop point for the live TUI cycle
- live slash-command discoverability is handled by the TUI palette, not by new runtime commands
- no new runtime command work belongs to this cycle
- future changes require a new phase

Post-closeout follow-up phases:

- P25 — Reconsider `agent` / `agent --record`
- P26 — Deprecate legacy bridges

## 9. Non-goals for this phase

This phase does not:

- add commands;
- change runtime behavior;
- touch RCK Core;
- migrate agent legacy behavior;
- deprecate bridges;
- create `ModelRouter`;
- write RCK for new paths;
- touch `packages/` or `.pi/`;
- blur proposal, validation, and materialization boundaries.

## 10. Relationship to the README

The `tools/rfs/README.md` file keeps a concise command-surface overview.
This document is the canonical governance reference for the phase.
